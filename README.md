```bash
$ terraform init

$ terraform apply

$ ansible-galaxy collection install community.general

$ ansible-galaxy install -r requirements.yml

$ ansible-playbook -i ansible/inventory ansible/boundary.yml --extra-vars "boundary_psql_password='84f405a372cfb22b29be43ce6174c2'"
```